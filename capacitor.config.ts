import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'novia_week10_crossmobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
